extends _layouts/base

block tags
  script(src="https://identity.netlify.com/v1/netlify-identity-widget.js")
  script.
    if (window.netlifyIdentity) {
    window.netlifyIdentity.on("init", user => {
    if (!user) {
    window.netlifyIdentity.on("login", () => {
    document.location.href = "/admin/";
    });
    }
    });
    }


block content
  .cover-art

  section.card.splash.splash-red.splash-right#terapia
    .base-width.pure-g
      .card-text.pure-u-1.pure-u-md-1-2.circle-red
        h2= site.data.main.about_title
        != marked(site.data.main.about_body)
      .card-photo.pure-u-1.pure-u-md-1-2
        .polaroid-stack
          .polaroid(style="background-image: url('/images/drabina2.jpg'); transform: rotate(-10deg);")
          .polaroid(style="background-image: url('/images/drabina2.jpg'); transform: rotate(10deg);")

  .divider
    .pure-g
        img.pure-u-1-1(src="/images/kids_big.jpg", alt="", style="width: 100%; height:100%; object-position: 50% 50%;")

  section.card.splash.splash-yellow.splash-left
    .base-width.pure-g
      .card-photo.pure-u-1.pure-u-md-1-2
        .polaroid-stack
          .polaroid(style="background-image: url('/images/dance.jpg'); transform: rotate(10deg);")
          .polaroid(style="background-image: url('/images/dance.jpg'); transform: rotate(-10deg);")
      .card-text.pure-u-1.pure-u-md-1-2.circle-yellow
        h2= site.data.main.sztuka_title
        != marked(site.data.main.sztuka_body)